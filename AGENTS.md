# AGENTS.md

## Vibe 行为准则

### 1. 模块自解释（One-Line Comment）
- 每一个新创建的函数、Hook 或重要组件，必须在定义上方写一行精简且直观的中文注释。
- 注释只解释“为了实现什么功能”，不要解释“怎么写”。

正确示例：
```ts
// 扫描指定目录下的 .exe 文件并提取图标与元数据
export const useAppScanner = (path: string) => { ... }
```

### 2. 变更先导（Plan First）
- 在执行任何 Bugfix 或新功能开发前，默认不要直接输出代码。
- 先输出以下内容：
  1. 对当前问题的理解
  2. 可能影响的文件或模块
  3. 实现步骤（Step 1, Step 2, Step 3...）
- 如果当前对话明显处于连续快速迭代阶段，可以在给出计划后直接继续给出修改方案，不必机械等待确认。
- 如果用户明确要求“直接改”或“直接给代码”，则遵循用户要求。

### 3. 差异化交付（Diff Mode）
- 除初始化项目外，后续更新优先使用 diff 格式交付。
- 如果不适合输出 unified diff，则必须清晰标注：
  - // Added:
  - // Modified:
  - // Removed:
- 目标是让用户一眼看出修改点，避免整文件重写掩盖变更。

### 4. 不要静默删除旧逻辑
- 不要偷偷删除用户已有逻辑。
- 如果必须删除旧逻辑，必须明确说明：
  1. 删除了什么
  2. 为什么要删
  3. 对现有行为有什么影响

### 5. 最小改动原则
- 优先做最小必要修改，避免无关重构。
- 不要顺手修改命名、格式化、目录结构或代码风格，除非这些改动与当前任务直接相关。
- 如果发现更大范围的结构问题，先说明，再由用户决定是否扩大改动范围。

### 6. 多文件修改时的输出顺序
- 如果涉及多个文件，先列出会修改哪些文件以及每个文件的目的。
- 然后再给出代码变更。

### 7. 默认输出模板

默认按下面顺序输出：
1. 问题理解
2. 修改计划
3. 影响文件
4. 代码变更（优先 diff）
5. 变更说明