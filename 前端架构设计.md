# LaunchManager · 前端架构设计文档

---

## 视觉设计规范

### 风格定位
Notion 风格：极简、克制、留白充足，无多余装饰。

### 色板（配置在 tailwind.config.js）

```js
cream: {
  bg:      '#FDFBF7',   // 全局背景
  surface: '#F5F0E8',   // 卡片、面板表面
  hover:   '#EDE4D3',   // 悬停态
  border:  '#E2D9CC',   // 分割线、描边
  accent:  '#C9A96E',   // 主操作按钮（启动、确认）
  danger:  '#D97B6C',   // 危险操作（关闭进程）
  textPrimary:   '#3D3530',
  textSecondary: '#9C8F85',
  textDisabled:  '#C4BAB3',
}
```

### 圆角规范
| 元素 | border-radius |
|---|---|
| 预设卡片 | 16px |
| 按钮（普通） | 10px |
| 按钮（图标方形） | 12px |
| 弹窗 / 面板 | 16px |
| 状态标签 / Badge | 999px（胶囊形） |
| 输入框 | 10px |

### 阴影规范
```css
--shadow-card:  0 2px 8px rgba(61,53,48,0.07);
--shadow-modal: 0 8px 32px rgba(61,53,48,0.14);
--shadow-hover: 0 4px 16px rgba(61,53,48,0.10);
```

### 字体
Inter（优先）或 system-ui，不使用任何衬线字体。

---

## 组件树

```
App
├── TopBar                          # 顶部栏
├── PresetList                      # 预设列表容器（可垂直滚动）
│   └── PresetCard[]                # 预设卡片（折叠 / 展开两种状态）
│       ├── PresetCardCollapsed     # 折叠态（默认）
│       │   ├── PresetName          # 预设名称（可点击重命名）
│       │   ├── AppIconRow          # 应用图标横排 + 省略号按钮
│       │   ├── StatusBadge         # 「X / Y 运行中」实时标签
│       │   └── ActionButtons       # ▶ 启动 + ■ 关闭 按钮
│       └── PresetCardExpanded      # 展开态（点击卡片空白区进入）
│           ├── PresetName          # 可重命名
│           ├── SortableAppGrid     # 可拖拽排序的图标网格（@dnd-kit）
│           │   ├── AppIcon[]       # 单个图标（点击弹出删除确认）
│           │   └── AddButton       # ＋ 按钮
│           └── AppPicker           # 应用选择面板（点击 + 后展开）
│               ├── SearchInput     # 搜索过滤
│               ├── AppPickerList   # 应用条目列表
│               │   └── AppPickerItem[]
│               └── CustomAppForm   # 自定义路径 + 图标上传
├── SettingsButton                  # 右下角设置入口（固定定位）
├── SettingsModal                   # 设置弹窗
│   ├── AdminModeToggle
│   ├── BackgroundToggle
│   └── StartupToggle
└── ConfirmDialog                   # 关闭高风险应用前的确认弹窗
```

---

## 组件详细说明

### TopBar
```
[ LaunchManager ]                    [ + 新建预设 ]
```
- 左侧：应用名称（纯文本，textPrimary，字重 600）
- 右侧：「+ 新建预设」按钮，accent 色，点击后在列表末尾插入空白预设并自动进入命名状态

---

### PresetCard — 折叠态（默认）

```
┌─────────────────────────────────────────────────────┐
│  办公模式                          ● 3 / 4 运行中    │
│                                                     │
│  [QQ] [微信] [Chrome] [Notion]  [···]               │
│                                       [▶启动] [■关闭] │
└─────────────────────────────────────────────────────┘
```

- 卡片背景：`cream.surface`，hover 时切换为 `cream.hover`
- **图标区**：48×48px，圆角 12px，横排，间距 8px，超出时末尾显示省略号按钮（同尺寸）
- **状态标签**（StatusBadge）：
  - 无运行：不显示
  - 部分运行：`● X / Y 运行中`，琥珀色背景
  - 全部运行：`● Y / Y 运行中`，绿色背景
- **启动按钮**：accent 色填充，圆角 10px
- **关闭按钮**：danger 色描边（非填充），圆角 10px，hover 时填充

---

### PresetCard — 展开态（编辑模式）

```
┌─────────────────────────────────────────────────────┐
│  办公模式  ✏                                         │
│                                                     │
│  [QQ] [微信] [Chrome] [Notion]  [＋]                │
│                                                     │
│  ┌─ 添加应用 ──────────────────────────────────────┐│
│  │ 🔍 搜索应用...                                   ││
│  │ [Spotify]  [Steam]  [Discord]  ...              ││
│  │ ─────────────────────────────                   ││
│  │ 找不到？→ 输入路径 / 上传图标                      ││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

- 点击卡片空白区域展开，点击外部或按 Esc 收起
- 展开时图标放大至 64×64px
- **拖拽排序**：使用 `@dnd-kit/core`，拖动时图标有轻微浮起阴影效果
- **点击图标**：出现小型 popover，显示「移除」按钮
- **AppPicker 面板**：嵌在卡片内部展开，非独立弹窗，减少层级感

---

### AppPicker（应用选择面板）

- **搜索框**：顶部，实时过滤列表
- **应用条目**：图标（32px）+ 应用名，点击后立即加入预设并关闭面板
- **已在预设内的应用**：不显示
- **支持拖拽**：可将系统文件管理器中的 `.exe` 或 `.lnk` 文件拖入此面板
- **底部分割线后**：CustomAppForm，两个字段——
  - 可执行文件路径（文本输入 + 「浏览」按钮）
  - 自定义图标（图片上传，显示预览缩略图）

---

### SettingsModal（设置弹窗）

- 右下角固定一个 ⚙ 齿轮图标按钮（圆形，`cream.surface` 背景，`cream.border` 描边）
- 点击后弹出 modal（居中，`cream.bg` 背景，`--shadow-modal`）
- 三个设置项，每项为一行：左侧文字说明，右侧 Toggle 开关

```
┌──────────────────────────────────┐
│  设置                          ✕ │
│  ─────────────────────────────   │
│  管理员模式（关闭顽固进程）    ◯  │
│  后台运行                      ●  │
│  开机自启                      ◯  │
└──────────────────────────────────┘
```

---

### ConfirmDialog（高风险关闭确认）

- 遮罩层 + 居中弹窗
- 内容：「以下应用可能有未保存的内容：Word、Notepad++，确认关闭？」
- 两个按钮：「取消」（描边）+ 「确认关闭」（danger 色填充）

---

### StatusBadge（状态徽标）

| 状态 | 样式 |
|---|---|
| 全部未运行 | 不渲染 |
| 部分运行 | 琥珀色背景 `#FEF3C7`，文字 `#92400E`，`● X / Y 运行中` |
| 全部运行 | 绿色背景 `#D1FAE5`，文字 `#065F46`，`● Y / Y 运行中` |

---

## Hooks 层

### `usePresets.ts`
- 职责：预设数据的 CRUD，通过 IPC 与主进程同步
- 暴露：`presets`, `createPreset`, `updatePreset`, `deletePreset`, `reorderPresets`

### `useProcessStatus.ts`
- 职责：监听 `monitor:statusUpdate` IPC 推送，维护每个 appId 的运行状态 Map
- 暴露：`statusMap: Map<appId, boolean>`，供 StatusBadge 消费

### `useAppList.ts`
- 职责：调用 `app:scanInstalled` IPC，获取并缓存已安装应用列表
- 触发时机：AppPicker 首次打开时懒加载

### `useSettings.ts`
- 职责：读写设置项，通过 IPC 与主进程同步
- 暴露：`settings`, `updateSettings`

---

## 文件结构

```
src/
├── main.tsx                        # React 渲染入口
├── App.tsx                         # 根组件，整体布局
│
├── components/
│   ├── TopBar.tsx
│   ├── PresetList.tsx
│   ├── PresetCard/
│   │   ├── PresetCard.tsx          # 状态容器，控制折叠/展开
│   │   ├── PresetCardCollapsed.tsx
│   │   ├── PresetCardExpanded.tsx
│   │   └── AppIconRow.tsx          # 图标横排组件（折叠态用）
│   ├── AppPicker/
│   │   ├── AppPicker.tsx
│   │   ├── AppPickerItem.tsx
│   │   └── CustomAppForm.tsx
│   ├── SortableAppGrid.tsx         # @dnd-kit 拖拽排序网格
│   ├── StatusBadge.tsx
│   ├── ActionButtons.tsx           # 启动 + 关闭按钮组
│   ├── SettingsButton.tsx          # 右下角齿轮入口
│   ├── SettingsModal.tsx
│   ├── ConfirmDialog.tsx
│   └── Toggle.tsx                  # 通用开关组件
│
├── hooks/
│   ├── usePresets.ts
│   ├── useProcessStatus.ts
│   ├── useAppList.ts
│   └── useSettings.ts
│
├── types/
│   └── index.ts                    # AppEntry, Preset, Settings, StatusMap 类型定义
│
└── styles/
    └── globals.css                 # Tailwind 指令 + CSS 变量声明
```

---

## 动效规范

| 交互 | 动效 |
|---|---|
| 卡片展开/收起 | 高度过渡，`transition: height 200ms ease` |
| AppPicker 展开 | `opacity + translateY`，150ms |
| 图标 hover | `scale(1.05)`，100ms |
| Modal 出现 | `opacity + scale(0.97→1)`，150ms |
| StatusBadge 颜色变化 | `transition: background-color 300ms` |

---
