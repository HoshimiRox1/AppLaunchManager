# LaunchManager · 后端架构设计文档

---

## 模块总览

```
┌─────────────────────────────────────────────────────┐
│                    React 前端 (IPC)                   │
└───────────────┬─────────────────────────────────────┘
                │ IPC 双向通信
┌───────────────▼─────────────────────────────────────┐
│                   IPC Bridge (preload.ts)             │
└───────────────┬─────────────────────────────────────┘
                │
        ┌───────▼────────┐
        │   主进程 (main) │
        └───────┬────────┘
                │ 调用
    ┌───────────┼───────────────────────┐
    │           │           │           │
┌───▼───┐ ┌────▼───┐ ┌─────▼────┐ ┌───▼───┐
│ 预设  │ │ 启动   │ │  监测    │ │ 设置  │
│ 模块  │ │ 模块   │ │  模块    │ │ 模块  │
└───────┘ └────┬───┘ └─────┬────┘ └───────┘
               │           │
               └─────┬─────┘
                 ┌───▼───┐
                 │ 关闭  │
                 │ 模块  │
                 └───┬───┘
                     │
                 ┌───▼───┐
                 │ 日志  │
                 │ 模块  │
                 └───────┘
```

---

## 一、预设模块 `preset-store.ts`

### 职责
管理预设的增删改查与持久化，不涉及任何进程逻辑。

### 数据结构

```typescript
interface AppEntry {
  id: string                  // uuid
  name: string                // 显示名称
  exePath: string             // 可执行文件完整路径（启动用）
  installDir: string          // 安装目录（监测用，自动从 exePath 推断，可手动修改）
  iconPath: string            // 图标路径（系统提取或用户上传）
  customProcessNames: string[] // 用户手动配置的额外进程名（兜底用）
}

interface Preset {
  id: string
  name: string
  order: number               // 控制 UI 排序
  apps: AppEntry[]
}
```

### 应用添加的三种方式（UI 层触发，模块层处理）
1. **搜索添加**：扫描注册表 + 开始菜单 + 桌面快捷方式，三路合并去重，返回应用列表
2. **拖拽添加**：接收拖入的 `.exe` 或 `.lnk` 文件路径，解析出 exePath 和 installDir
3. **路径输入**：用户手填 exePath，模块自动推断 installDir（取 exePath 的上一级目录）

### 持久化
- 使用 `electron-store`，存入 `presets.json`
- 每次修改后立即写入，应用启动时自动加载

---

## 二、设置模块 `settings-store.ts`

### 职责
管理全局设置项的读写与系统级功能的开关。

### 数据结构

```typescript
interface Settings {
  adminMode: boolean          // 关闭操作是否使用管理员权限（runas 提权，非整体提权）
  runInBackground: boolean    // 关闭主窗口后是否保留托盘
  launchOnStartup: boolean    // 开机自启
}
```

### 实现说明
- `adminMode`：不提升整个 Electron 进程权限，仅在关闭模块执行 `taskkill` 时通过 PowerShell `Start-Process -Verb RunAs` 单独提权，避免 UAC 弹窗影响正常使用
- `launchOnStartup`：通过 Electron 的 `app.setLoginItemSettings()` 实现
- `runInBackground`：监听主窗口 `close` 事件，根据此设置决定 `hide()` 还是 `quit()`

---

## 三、应用启动模块 `launcher.ts`

### 职责
根据 AppEntry 启动应用，并将启动结果（成功/失败 + 初始 PID）交给监测模块。

### 启动策略（按优先级，失败自动降级）

```
优先：child_process.spawn(exePath)
  ↓ 失败
降级：通过 .bat 文件执行 cmd /c start "" "exePath"
```

`spawn` 作为主力是因为它能同步拿到 PID，PID 是监测模块进程树追踪的起点。
`.bat` 方式作为保底，兼容某些必须通过 shell 启动的应用。

### 启动流程

```
1. 调用 spawn / bat 启动应用
2. 记录返回的初始 PID（spawn 方式可直接获取）
3. 等待 500ms
4. 将 { appId, initialPid, installDir, customProcessNames } 交给监测模块注册
5. 将启动结果（成功/失败）写入日志模块
```

### 批量启动
- 预设内的应用按 order 顺序依次启动
- 每个应用间隔 800ms，防止瞬间卡机
- 已处于运行中状态的应用直接跳过

---

## 四、应用监测模块 `process-monitor.ts`（核心）

### 职责
维护每个已启动应用的运行状态，实时推送给前端。

### 四层检测机制（任意一层命中 = Running）

#### 第一层：安装目录路径轮询（主力）
```powershell
Get-CimInstance Win32_Process |
  Where-Object { $_.ExecutablePath -ne $null } |
  Select-Object ProcessId, Name, ExecutablePath |
  ConvertTo-Json
```
遍历结果，只要某进程的 `ExecutablePath` 以应用的 `installDir` 开头，即判定为运行中。
**覆盖场景**：进程名变了，但还在同一目录下的所有情况。

#### 第二层：进程树追踪（启动时建立）
- 应用启动后立即用 `initialPid` 做一次进程树快照
- 记录所有直接和间接子进程的 PID
- 父进程退出后，继续监听子进程状态
- **覆盖场景**：启动器（父进程）退出后由子进程接管的情况（如鸣潮）

#### 第三层：窗口关联检测
- 通过 `node-window-manager` 枚举可见窗口
- 获取每个窗口的 PID，解析其可执行文件路径
- 路径匹配 `installDir` 即判定为运行中
- **覆盖场景**：路径轮询偶发失败、进程路径获取不到的情况

#### 第四层：自定义进程名检测（兜底）
- 读取 AppEntry 的 `customProcessNames` 字段
- 直接按进程名匹配
- **覆盖场景**：以上三层全部失效的极端情况，或用户已知进程名时的快速配置

### 轮询设计
```
轮询间隔：3 秒
每次轮询：四层并发执行，取 OR 结果
结果变化时：通过 IPC 推送新状态给前端
结果未变化：不推送，减少 IPC 开销
```

### 已知 PID 缓存
```typescript
interface MonitorEntry {
  appId: string
  installDir: string
  customProcessNames: string[]
  trackedPids: Set<number>     // 进程树追踪收集到的所有 PID
  isRunning: boolean           // 上一次的状态，用于变更检测
}
```

---

## 五、应用关闭模块 `killer.ts`

### 职责
接收监测模块提供的运行数据，彻底关闭目标应用的所有相关进程。

### 输入（来自监测模块）
```typescript
interface KillTarget {
  appId: string
  trackedPids: number[]        // 监测模块收集的所有 PID
  processNames: string[]       // 监测模块命中的所有进程名
  installDir: string           // 安装目录，用于路径过滤关闭
}
```

### 关闭策略（三路并发执行）

```
1. 按 PID 关闭（最精准）
   tree-kill(pid) 对 trackedPids 中每个 PID 执行

2. 按进程名关闭
   taskkill /F /IM <processName> 对 processNames 中每个名称执行

3. 按安装目录路径关闭（兜底）
   PowerShell:
   Get-CimInstance Win32_Process |
     Where-Object { $_.ExecutablePath -like "installDir\*" } |
     Stop-Process -Force
```

### 管理员权限处理
- `adminMode = false`：直接执行上述命令
- `adminMode = true`：将第 2、3 步的命令包装为 `Start-Process powershell -Verb RunAs -ArgumentList ...` 单独提权执行

### 关闭前校验（高风险应用提示）
- 内置高风险进程名列表：`winword.exe, excel.exe, powerpnt.exe, notepad++.exe, code.exe` 等
- 关闭前检查 `processNames` 是否与列表有交集
- 有交集 → 通过 IPC 通知前端弹出确认弹窗，用户确认后再执行
- 无交集 → 直接执行

---

## 六、日志模块 `logger.ts`

### 职责
统一记录所有模块的运行日志，持久化到本地文件。

### 推荐库
`electron-log`（轻量，无需配置，自动存到 `%APPDATA%/LaunchManager/logs/`）

### 日志级别规范

| 级别 | 使用场景 |
|---|---|
| `info` | 应用启动成功、关闭成功、状态变更 |
| `warn` | 降级到 bat 启动、某一层检测失败但其他层成功 |
| `error` | 启动失败、关闭失败、PowerShell 命令执行异常 |

### 必须覆盖 try-catch 的位置
- 监测模块：每次轮询整体包裹，单次失败只写 warn，不中断轮询
- 关闭模块：每路关闭策略独立包裹，某路失败继续执行其他路
- 启动模块：spawn 失败时写 warn 并降级，bat 失败时写 error

### 日志格式示例
```
[2025-01-15 14:23:01] [INFO]  [Launcher]  鸣潮启动器 launched, PID=12453
[2025-01-15 14:23:04] [INFO]  [Monitor]   鸣潮启动器 status: Running (Layer 1 - path match)
[2025-01-15 14:25:11] [INFO]  [Killer]    鸣潮启动器 kill initiated
[2025-01-15 14:25:12] [WARN]  [Killer]    taskkill by name failed, falling back to path-based Stop-Process
[2025-01-15 14:25:13] [INFO]  [Killer]    鸣潮启动器 all processes terminated
```

---

## 模块间依赖关系

```
preset-store    ──→  launcher
                          │
                          ▼
                    process-monitor  ──→  前端状态推送
                          │
                          ▼
                        killer
                          │
                    ┌─────┴──────┐
                    ▼            ▼
               logger        settings-store
               （所有模块均依赖）
```

---

## IPC 通道清单

| 通道名 | 方向 | 说明 |
|---|---|---|
| `preset:getAll` | 主→渲染 | 获取所有预设 |
| `preset:save` | 渲染→主 | 保存预设变更 |
| `launcher:start` | 渲染→主 | 启动某预设 |
| `monitor:statusUpdate` | 主→渲染 | 推送运行状态变更 |
| `killer:stop` | 渲染→主 | 关闭某预设 |
| `killer:confirmAndStop` | 渲染→主 | 用户确认后执行关闭 |
| `settings:get` | 主→渲染 | 获取设置 |
| `settings:save` | 渲染→主 | 保存设置 |
| `app:scanInstalled` | 渲染→主 | 获取已安装应用列表 |

---
